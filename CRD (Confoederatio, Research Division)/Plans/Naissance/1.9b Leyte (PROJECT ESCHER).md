**Project Escher** (May-June/July 2026) takes place before SVEA II in order to lay the technical groundwork for more complex changes over time.

**Note.** The codebase is still lightweight and agile enough that any major changes should be implemented at this stage prior to horizontal expansion. **Slow and steady now wins the race.**

A full recode of **Naissance HGIS** is underway to help improve performance and future flexibility.
## Immediate Steps.

1. Create a new personal GitHub repository outside the main Confoederatio organisation. This repository should be labelled 'ReNaissance', since it is a complete recode of Naissance as it exists.
2. Port over the current gutted multithreading branch from SVEA to here, but leave out histmap/livemap functionality for now.
3. Revert Collation/SVEA back to the singlethreaded branch for continued updates, experimentation, and maintenance as needed. They can be folded into the new engine as needed.
# Data Structure.

**1. Render** Thread
- data/
	- naissance.Brush:
		- naissance.BrushNodeEditor
	- naissance.Entity:
		- static instances: naissance.Entity[]
		- naissance.Feature:
			- naissance.FeatureGroup
			- naissance.FeatureLayer - Should actually bind to a maptalks Layer.
			- naissance.FeatureTileLayer
		- naissance.Geometry:
			- naissance.GeometryLine
			- naissance.GeometryPoint
			- naissance.GeometryPolygon
	- naissance.Mapmode - Each mapmode should return and use its own maptalks Layer.
	- naissance.Renderer (static):
		- naissance.Renderer.draw()
- `ui/`
	- `_components/`
		- Component_Search - Returns both placename results in search as well as `naissance.Entity` instances that meet the criteria.
	- `bottombar/`
		- UI_Brush
	- `leftbar/`
		- UI_Leftbar
	- `map/`
		- UI_MapContextMenu
	- `rightbar/`
		- UI_Timeline
	- `topbar/`
		- UI_DateMenu
	- `windows/`
		- UI_MapSettingsWindow
		- UI_SettingsWindow
		- UI_SystemManagerWindow

**2. Process** Threads
- naissance.Action:
	- naissance.FeatureAction - Performs multiple naissance.GeometryActions over an entire set, but only appends to DALS once.
	- naissance.GeometryAction

**3. DB** Threads
- naissance.History (static)

### DALS to be refactored:

Classes of actions are separated out so they can be marked and compiled as a whole for tracking. Render-side actions do not yet require refactoring. Brush actions should only be added to Geometry once the Geometry is finished.

**Geometries:** These are the most important since Feature actions are made of multiple Geometry actions executed in sequence.
- Geometry_actions
- GeometryLine_actions
- GeometryPoint_actions
- GeometryPolygon_actions

**Features:**
- Feature_actions
- FeatureGroup_actions
- FeatureLayer_actions
- FeatureSketchMap_actions
- FeatureTileLayer_actions